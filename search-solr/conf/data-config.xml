<?xml version="1.0" encoding="UTF-8"?>
<dataConfig>
    <dataSource 
        type="JdbcDataSource" 
        driver="com.mysql.jdbc.Driver"
        url="jdbc:mysql://db-news:3306/rediris?useUnicode=true&amp;characterEncoding=utf8&amp;useSSL=false&amp;allowPublicKeyRetrieval=true"
        user="news_user" 
        password="news_password"
    />
    
    <document>
        <!-- tech_docs 테이블에서 데이터 가져오기 -->
        <entity name="tech_docs" 
                query="SELECT doc_id as id, type, title, content, author, DATE_FORMAT(created_date, '%Y-%m-%dT00:00:00Z') as date FROM tech_docs"
                deltaQuery="SELECT doc_id as id FROM tech_docs WHERE created_date > '${dataimporter.last_index_time}'"
                deltaImportQuery="SELECT doc_id as id, type, title, content, author, DATE_FORMAT(created_date, '%Y-%m-%dT00:00:00Z') as date FROM tech_docs WHERE doc_id='${dataimporter.delta.id}'">
            
            <field column="id" name="id" />
            <field column="type" name="type" />
            <field column="title" name="title" />
            <field column="content" name="content" />
            <field column="author" name="author" />
            <field column="date" name="date" />
        </entity>
    </document>
</dataConfig>
